version: '3'
services:
  api:
    build: ./api/
    container_name: api
    env_file:
      - ./api/.env
    restart: unless-stopped
    ports:
      - 4000:4000
    networks:
      - network
  context_aware:
    build: ./context_aware/
    container_name: context_aware
    networks:
      - network

networks:
  network:
    driver: bridge
